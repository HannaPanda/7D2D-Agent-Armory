# .NET build output (DLL source)
[Bb]in/
[Oo]bj/
out/
*.user

# Backups / temp
*.dll.bak
*.bak
*.bak-*
*.tmp

# Unity project (only the exported source PNGs + scripts live in src/texture)
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Ll]ogs/
[Uu]serSettings/
*.csproj.user

# OS
Thumbs.db
desktop.ini
.DS_Store

# Release zips are built by CI, never committed.
dist/

# `nexus/` is NOT ignored: the mod-page description hotlinks nexus/images/* straight
# out of this repo via raw.githubusercontent.com, so those files have to be pushed
# or the live description shows broken images.
